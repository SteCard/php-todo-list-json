"use strict";

// RICHIAMO FUNZIONE CREATE_APP
const { createApp } = Vue;

// FUNZIONE CREATE_APP
createApp({

    data() {
        return {
        }
    }
}).mount('#app'); // RICHIAMO HTML PER VEDERE LA